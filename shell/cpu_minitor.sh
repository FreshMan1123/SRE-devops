#你需要监控服务器的CPU使用率。当CPU使用率超过80%时，需要记录告警信息并发送通知。
#具体要求：
#每隔30秒检查一次CPU使用率
#使用top命令获取CPU使用率（提取idle值并计算使用率）
###输出告警信息："CPU使用率过高: XX.X%"
#将告警信息追加写入到/tmp/cpu_alert.log文件
#记录当前时间戳
#脚本需要能够持续运行（无限循环）
#使用bc命令进行浮点数比较
#脚本接收一个参数作为告警阈值（默认80.0）
#提供优雅退出机制（捕获SIGINT信号）

top -bn1 | awk '{ if(NR>=2){if($4>80){print($1,$4)}}}' | while read -r line;do
    